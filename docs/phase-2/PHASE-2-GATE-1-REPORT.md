# Phase 2 — Gate 1: Merchant Identity

**Status: PASS / CLOSED**

Implemented:
- merchant email/password registration and login;
- logout;
- persisted browser session;
- Firebase ID-token refresh;
- protected merchant UI boundary;
- server-authoritative merchant provisioning;
- Firebase project binding to `loyal-hub-project`.

The web API key remains environment-only. Production deployment evidence is not claimed until an authenticated Firebase deployment is executed.
